import { useContext } from "react";

import Context from "./EventCardFiltersModalContext";

const useEventCardFiltersModalContext = () => useContext(Context);

export default useEventCardFiltersModalContext;
